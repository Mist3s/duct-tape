"""Единый класс фатальной ошибки задания (раньше был продублирован в каждом скрипте)."""

from __future__ import annotations


class JobError(Exception):
    """Фатальная ошибка задания — некорректные входные данные и т.п.

    Отличается от прочих исключений тем, что её текст предназначен пользователю:
    CLI печатает его в stderr, GUI показывает в messagebox без трассировки стека.
    """
