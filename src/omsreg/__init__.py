"""omsreg — инструменты обработки DBF-реестров ОМС с единым графическим интерфейсом.

Пакет состоит из трёх слоёв:

  omsreg.core   — общий, не зависящий от GUI код: чтение/запись DBF, преобразование
                  значений, логирование задач, резервное копирование, разбор текста.
  omsreg.utils  — доменные утилиты (удаление ошибочных талонов, удаление по списку
                  кодов, статистика стационара). Каждая пригодна и как CLI, и как
                  импортируемая функция run_*.
  omsreg.gui    — платформа: реестр утилит + автоматическая сборка вкладок и хранения
                  настроек из декларативных спецификаций (omsreg.gui.spec).

Новая утилита добавляется одним файлом-плагином в omsreg.gui.plugins — интерфейс,
персистентность настроек и поток запуск/проверка/подтверждение/журнал берутся из схемы.
"""

__version__ = "1.1.2"

__all__ = ["__version__"]
