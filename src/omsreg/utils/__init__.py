"""omsreg.utils — доменные утилиты обработки реестров ОМС.

Сгруппированы по доменам в подпакеты:
  removal — удаление записей из DBF (error_talons — по протоколам, codes — по списку кодов);
  stat    — отчёты по стационару (stacionar — статистика, economics — экономика).

Каждый инструмент даёт функцию main() (CLI-команда) и импортируемую run_* — именно её
оборачивают плагины графического интерфейса. Общий код домена — в модулях common
соответствующего подпакета, генерация отчётов — в модулях *_report. Вся работа с DBF,
логированием и резервным копированием берётся из omsreg.core.
"""
