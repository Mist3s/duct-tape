"""Плагины утилит. Каждый модуль объявляет объект SPEC (omsreg.gui.spec.UtilitySpec);
omsreg.gui.registry.discover() находит их автоматически. Чтобы добавить утилиту —
положите сюда новый модуль с SPEC, менять другие файлы не нужно."""
