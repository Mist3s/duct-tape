"""omsreg.gui — графическая платформа: реестр утилит + автосборка интерфейса.

Утилита описывается декларативной спецификацией (omsreg.gui.spec.UtilitySpec):
метаданные + схема параметров + кнопки-действия + функция run. Из этой схемы
приложение (omsreg.gui.app) само строит вкладку, сохраняет/читает настройки и
проводит единый поток запуск/проверка/подтверждение/журнал. Добавить утилиту —
значит положить один файл-плагин в omsreg.gui.plugins с объектом SPEC.

Модуль импортирует tkinter, поэтому вне графической среды его не трогают
(ядро и утилиты от него не зависят).
"""
