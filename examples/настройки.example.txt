# Пример файла настроек программы «Синяя изолента».
# Скопируйте рядом с программой под именем «настройки.txt» (или просто запустите
# программу — она создаст его сама). Формат: ключ = значение. Пути оставлены пустыми.

# --- Программа ---
# Проверять выход новой версии при запуске (1 — да, 0 — нет).
app.check_updates = 1

# --- Удаление по протоколам ---
talons.dir =
talons.common =
talons.field = KOD_TALON

# --- Удаление по списку кодов ---
codes.dir =
codes.codes_file =
codes.field = KOD_TALON
codes.min_len = 6
codes.max_len = 12

# --- Статистика стационара ---
stat.target =
stat.day_kotd = 10,15,12
stat.field_kotd = KOTD
stat.field_kmkb = KMKB
stat.field_stoim = STOIM
stat.field_ishod = ISHOD
stat.field_fact = FACT

# --- Экономика стационара ---
economics.target =
economics.day_kotd = 10,15,12

# --- Смертность стационара ---
deaths.target =
# свой справочник МКБ->группа; пусто — встроенный mkb_death_groups.csv
deaths.groups =
# 1 — патологоанатомический диагноз приоритетнее клинического
deaths.patho_priority = False
